#include <stdio.h>
#include <stdlib.h>

/*
 * Declare a variable called isFemale and assign an appropriate 
 * value corresponding to your gender. Print it on the console.
 */
int main() 
{
    _Bool isFemale = 0;
    
    printf("Am I female? : %s\n", isFemale ? "true" : "false");
    
    return (EXIT_SUCCESS);
}